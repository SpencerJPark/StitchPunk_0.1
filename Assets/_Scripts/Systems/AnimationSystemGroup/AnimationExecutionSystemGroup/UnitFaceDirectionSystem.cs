
// To change animation (with optional blend)
/*public void SetAnimation(ref CharacterAnimation anim, AnimationType newAnim, float blendTime = 0f)
{
    anim.requestedAnimation = newAnim;
    anim.blendDuration = blendTime;
}

// To enable a facing direction overlay
public void SetFacingDirection(ref CharacterAnimationLayer layer, bool faceLeft)
{
    layer.animation = faceLeft ? AnimationType.FaceLeft : AnimationType.FaceRight;
    layer.active = true;
    layer.weight = 1f;
    layer.time = 0f;
}
*/