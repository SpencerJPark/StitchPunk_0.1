
    public enum ActionMaps
    {
        Player,       // on-foot controls
        Vehicle,      // vehicle controls
        PlayerZoom,   // zoomed-in camera controls (still on-foot actions)
        ControlUnits,        // (if you have a horde mode)
        MapUI         // (if you have a map UI mode)
    }
