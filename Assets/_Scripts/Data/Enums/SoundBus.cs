// Mixer routing bus for a sound. UI and Dialogue are reserved (not wired in v1).
public enum SoundBus : byte
{
    SFX,
    Ambient,
    Music,
    UI,        // reserved
    Dialogue,  // reserved
}
