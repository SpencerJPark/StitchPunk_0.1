public enum StructureType
{
    None            = 0,
    PrepTable       = 1,   // conditions a raw corpse body
    AssemblyBench   = 2,   // combines parts into an automaton frame
    GalvanicCharger = 3,   // charges the frame with electricity
    OutputBay       = 4,   // finished automata wait here for deployment
}
