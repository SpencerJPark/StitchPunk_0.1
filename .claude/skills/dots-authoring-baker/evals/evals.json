{
  "skill_name": "dots-authoring-baker",
  "evals": [
    {
      "id": "corpse-cart",
      "prompt": "Scaffold a Unity DOTS authoring component for Stitch Punk called CorpseCartAuthoring under Assets/_Scripts/Authoring/Structures/. Inspector fields: `public int capacity = 4;`, `public float pushSpeed = 2.0f;`, `public StructureType structureType;`. The baker should bake a `CorpseCart` IComponentData (with `capacity` and `pushSpeed`), a `ProductionProgress` enableable component that starts disabled, and a `DynamicBuffer<CarriedCorpse>` (left empty). Write only the C# file content — a single .cs file.",
      "expected_output": "A single .cs file. CorpseCartAuthoring MonoBehaviour + nested Baker, TransformUsageFlags.Dynamic, AddComponent+SetComponentEnabled pair for ProductionProgress disabled, AddBuffer<CarriedCorpse>, no var, no inline IComponentData struct declarations.",
      "files": []
    },
    {
      "id": "trap-so",
      "prompt": "Scaffold a Unity DOTS authoring component for Stitch Punk called TrapAuthoring. Inspector fields: `public TrapSO trapConfig;` (a ScriptableObject), `public float armTime = 2.0f;`. The baker should bake a `Trap` IComponentData with `armTime` and a `UnityObjectRef<TrapSO>` field named `config`, plus two enableable components that both start disabled: `TrapArmed` and `OnTrapTriggered`. Make sure incremental baking picks up edits to the SO. Write only the C# file.",
      "expected_output": "A single .cs file. DependsOn(authoring.trapConfig) called, UnityObjectRef<TrapSO> assigned via authoring.trapConfig, both enableable components baked disabled via SetComponentEnabled<T>(entity, false), correct TransformUsageFlags, no var, no inline struct declarations.",
      "files": []
    },
    {
      "id": "waypoint-registry",
      "prompt": "Scaffold a Unity DOTS authoring component for Stitch Punk called WaypointRegistryAuthoring. It's a data-only scene singleton that exposes `public GameObject[] waypoints;` and bakes a `WaypointRegistryTag` IComponentData (empty marker) plus a `DynamicBuffer<WaypointRef>` containing one entry per waypoint GameObject, where each entry stores the baked Entity reference for that waypoint. Write only the C# file.",
      "expected_output": "A single .cs file. TransformUsageFlags.None for the registry entity itself (no transform needed), TransformUsageFlags.Dynamic for each referenced waypoint GO via GetEntity(GameObject, ...), AddBuffer<WaypointRef> populated in a loop, no var, no inline IComponentData/IBufferElementData struct declarations in this file.",
      "files": []
    }
  ]
}
