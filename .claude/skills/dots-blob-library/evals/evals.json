{
  "skill_name": "dots-blob-library",
  "evals": [
    {
      "id": "item-library",
      "prompt": "Scaffold a new Stitch Punk blob library called ItemLibrary. The per-entry ScriptableObject is `ItemSO` with three fields: `public ItemType itemType;`, `public int baseValue;`, `public float weightPerUnit;`. The wrapper SO is `ItemLibrarySO` holding `public ItemSO[] items;`. The library should be readable from Burst jobs via a `BlobAssetReference<ItemLibraryBlob>`, where each entry holds those three scalar fields. Include an `ItemLibraryBlob.FindIndex(ItemType)` lookup helper. Write every C# file needed to wire this end-to-end into the existing Stitch Punk pipeline — entries, blob structs, library + reference components, authoring + baker, and baking system. You may output multiple .cs files, one per file you would add to the project.",
      "expected_output": "Six code artifacts: ItemSO (per-entry), ItemLibrarySO (wrapper), ItemBlobs.cs (ItemEntryBlob + ItemLibraryBlob with FindIndex), ItemLibrary + ItemLibraryReference components, ItemLibraryAuthoring with nested Baker (TransformUsageFlags.None, adds reference + empty library), and ItemLibraryBakingSystem in PostBakingSystemGroup with [WorldSystemFilter(BakingSystem)], BlobBuilder at Allocator.Temp, CreateBlobAssetReference at Allocator.Persistent, IsCreated-dispose guard on reassign, OnDestroy dispose loop.",
      "files": []
    },
    {
      "id": "upgrade-library-nested",
      "prompt": "Scaffold a new Stitch Punk blob library called UpgradeLibrary. Per-entry SO is `UpgradeSO` with: `public UpgradeType upgradeType;`, `public float multiplier;`, and `public FactoryItemType[] costItems;` (variable length per upgrade). Wrapper SO is `UpgradeLibrarySO` with `public UpgradeSO[] upgrades;`. The blob should expose each entry's `upgradeType`, `multiplier`, and a per-entry `BlobArray<FactoryItemType> costItems`. Provide a `FindIndex(UpgradeType)` helper. Write every C# file needed to wire this into the Stitch Punk pipeline.",
      "expected_output": "Six code artifacts. Key detail: the per-entry `costItems` nested BlobArray must be allocated off the outer BlobBuilder with `builder.Allocate(ref entriesBuilder[i].costItems, count)` — NOT via `root.entries[i]`. Null-count guard for empty costItems. Baking system has the full [WorldSystemFilter(BakingSystem)] + PostBakingSystemGroup + OnCreate RequireForUpdate + dispose-on-reassign + OnDestroy shape.",
      "files": []
    },
    {
      "id": "quip-library",
      "prompt": "Scaffold a new Stitch Punk blob library called QuipLibrary for short one-liner NPC barks. Per-entry SO is `QuipSO` with: `public BrainType speaker;`, `public string quipId;` (a short identifier), `public float cooldownSeconds;`. Wrapper SO: `QuipLibrarySO` with `public QuipSO[] quips;`. The blob should be Burst-readable — so the `quipId` needs to be a fixed-size string (pick a reasonable size). Provide a `QuipLibraryBlob.FindIndexById(in FixedString*Bytes quipId)` helper. Write every C# file needed to wire this in.",
      "expected_output": "Six code artifacts. Key detail: `quipId` becomes a `FixedString*Bytes` (from Unity.Collections) inside the entry blob, not System.String. FindIndexById helper uses `.Equals(quipId)` on the FixedString. All other baking-system invariants hold (BakingSystem world filter, PostBakingSystemGroup, Temp/Persistent allocators, dispose-on-reassign, OnDestroy cleanup).",
      "files": []
    }
  ]
}
